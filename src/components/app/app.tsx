import { AppHeader } from '../app-header/app-header';
import { RoutesComponent } from '../routes/routes-component';

export const App = () => {
  return (
    <div>
      <AppHeader/>
      <RoutesComponent />
    </div>
  );
}
